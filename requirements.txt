feedparser>=6.0
fpdf>=1.7
pandas>=2.0
python-dotenv>=1.0
pytest>=8.0
schedule>=1.2
slack-sdk>=3.0        # optionnel — alertes Slack
gpt4all>=2.0          # optionnel — résumé LLM local
pytest>=8.0           # développement / tests
